<%= form_for(current_user.liking_likes.find_by(liked_id: micropost.id),
                                            html: { method: :delete },
                                            remote: true) do |f| %>
    <%= f.submit "Unlike", class: "btn btn-primary btn-sm" %>
<% end %>