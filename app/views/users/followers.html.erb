<h1>フォロワー</h1>
<h2><%= @followers.count %>人</h2>
<% if @followers.any? %>
    <% @followers.each do |follower| %>
        <div class="following_follower">
            <%= link_to gravatar_for(follower, size: 50), follower %><%= follower.name %>
        </div>
    <% end %>
<% end %>